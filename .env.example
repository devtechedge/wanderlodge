# Optional. Herb identifier, lodge Q&A, and adventure copy fall back to
# canned demo payloads when this is unset. Never commit a real key.
GEMINI_API_KEY=

# Public URL of this app. Used only for self-referential links.
APP_URL=http://localhost:3000
